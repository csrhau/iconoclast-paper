\begin{tabular}{@{}<%= 'c'*data.headers.length -%>@{}} \toprule
<%= data.headers.join(' & ') %> \\
\midrule
<% data.each do |row| -%>
  <%= row.collect{|k, v| sprintf('%.2f', v) rescue v}.join(' & ') %> \\
<% end -%>
\bottomrule
\end{tabular}
