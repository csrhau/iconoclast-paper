\begin{tabular}{@{}l<%= 'r'*(data.headers.length-1+1) -%>@{}} \toprule
<%= data.headers.join(' & ') %> & $E^1t^2$ \\
\midrule
<% data.each do |row| -%>
  <%= row.map{|k, v| Integer(v) rescue sprintf('%.2f', v) rescue v}.join(' & ') %> & <%= (Float(row['Energy (J)']) * Float(row['Runtime (s)'])**2).round(0) %> \\
<% end -%>
\bottomrule
\end{tabular}
